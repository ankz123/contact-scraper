services:
  - type: web
    name: contact-scraper
    env: python
    buildCommand: playwright install && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    
